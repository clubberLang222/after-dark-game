# Full local tree

One-click package includes production dist + source.

```bash
cd AfterDark-OneClick-Install/game
git init
git remote add origin https://github.com/clubberLang222/after-dark-game.git
git add .
git commit -m "Full After Dark with 3D board textures"
git push -u origin main --force
```

Repo: https://github.com/clubberLang222/after-dark-game
